#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int n;
    int arr[1000];
    cin>>n;
    int i=0;
    while(i<n){
        cin>> arr[i];
    i++;
    }
    for(int j=(n-1);j>=0;j--){
        cout<<arr[j]<<" ";
    }
       
    return 0;
}
